import React from 'react';
import { Spinner } from '@blueprintjs/core';

import './Loader.scss';

const Loader = () => <Spinner className="loader" size={Spinner.SIZE_SMALL} />;

export default Loader;
