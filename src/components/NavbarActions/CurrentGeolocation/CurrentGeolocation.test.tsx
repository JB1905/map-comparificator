import { render } from '@testing-library/react';

import CurrentGeolocation from '.';

// TODO
describe('CurrentGeolocation', () => {
  it.skip('should', () => {
    const {} = render(<CurrentGeolocation />);
  });
});
