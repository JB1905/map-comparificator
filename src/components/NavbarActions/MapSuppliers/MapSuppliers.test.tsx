import { render } from '@testing-library/react';

import MapSuppliers from '.';

// TODO
describe('MapSuppliers', () => {
  it.skip('should', () => {
    const {} = render(<MapSuppliers />);
  });
});
