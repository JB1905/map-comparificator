import { render } from '@testing-library/react';

import CenteringModes from '.';

// TODO
describe('CenteringModes', () => {
  it.skip('should', () => {
    const {} = render(<CenteringModes />);
  });
});
