import React from 'react';
import { render } from '@testing-library/react';

import DeletePattern from '.';

describe('DeletePattern', () => {
  it('should render', () => {
    const {} = render(<DeletePattern />);

    expect();
  });
});
