import React from 'react';
import { render } from '@testing-library/react';

import NavbarSecondaryGroup from '.';

describe('NavbarSecondaryGroup', () => {
  it('should render', () => {
    const {} = render(<NavbarSecondaryGroup />);

    // expect();
  });
});
