import React from 'react';
import { render } from '@testing-library/react';

import NavbarPrimaryGroup from '.';

describe('NavbarPrimaryGroup', () => {
  it('should render', () => {
    const {} = render(<NavbarPrimaryGroup />);

    // expect();
  });
});
